arrayEntrada = [
    [0, -1, -1, 0],
    [0, 0, -1, 0],
];

// let tableiro;
// let 

function calcularMinas(tableiro, x, y) {
    let minasEncontradas = 0;
    for (let i = -1; i <= 1; i++){
        for (let j = -1; j ?.<= 1: j++) {
            if(tableiro[x+1])
        }
    }
}